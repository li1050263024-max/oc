# OC 设定抽卡器 — 需求文档

## 一、项目概述

### 1.1 项目名称
像素风 OC 设定抽卡器

### 1.2 产品形态
**微信小程序**。在微信内打开使用，无需安装，便于分享与二次打开。

### 1.3 项目简介
一款以像素美术风格呈现的 **微信小程序** 随机抽卡工具，用于随机生成 OC（Original Character，原创角色）的设定要素。用户通过「抽卡」获得包含姓名、种族、发色、瞳色、性格与怪癖在内的完整角色设定，结果以卡片形式展示，适合创作者获取灵感或进行角色设定练习。

### 1.4 目标用户
- 同人/原创写手、画手
- 跑团/TRPG 玩家需要快速生成 NPC
- 喜欢随机生成与抽卡体验的创作者

### 1.5 核心价值
- **随机性**：降低「想不出设定」的决策负担，用随机结果激发灵感  
- **完整性**：一次抽卡得到多维度设定，便于直接使用或二次加工  
- **体验**：像素风 UI + 抽卡反馈，提升使用趣味性  

---

## 二、功能需求

### 2.1 抽卡内容（随机维度）

每次抽卡从以下 **6 个维度** 各随机选取 **1 项**，共同组成一张「OC 设定卡」：

| 维度     | 说明           | 示例（需在系统中维护选项池）     |
|----------|----------------|----------------------------------|
| **OC 姓名** | 随机生成或随机选取的名字 | 如：艾莉娅、墨羽、林小满……     |
| **种族**   | 角色所属种族/物种     | 人类、精灵、兽人、机械、魔族…… |
| **发色**   | 头发颜色             | 黑、金、银白、渐变粉、薄荷绿…… |
| **瞳色**   | 眼睛颜色             | 琥珀色、异瞳、深红、冰蓝……     |
| **性格**   | 主要性格特质         | 外冷内热、话痨、腹黑、天然呆…… |
| **怪癖**   | 小习惯或特殊癖好     | 咬笔、数楼梯、收集纽扣……       |

> **说明**：每个维度的具体选项数量与内容需在需求或设计阶段确定，并在实现时以数据（如 JSON/配置文件）维护，便于后续扩展。

### 2.2 抽卡流程

1. **入口**：用户进入页面后，可见主界面（像素风背景 + 标题 + 抽卡按钮）。
2. **触发抽卡**：用户点击「抽卡」或类似按钮。
3. **随机逻辑**：系统对上述 6 个维度分别做一次随机选取（无重复要求，即同一维度每次只选 1 项）。
4. **结果展示**：将 6 个维度的结果组合为一张「OC 设定卡」，在界面中展示。
5. **可重复**：支持多次点击抽卡，每次重新随机并刷新展示结果。

### 2.3 结果展示需求

- **形式**：以「卡片」形式展示，卡片上清晰列出 6 个维度及其对应结果。
- **信息呈现**：
  - 维度名称（如「姓名」「种族」「发色」等）与对应随机结果需一一对应、易读。
  - 可选：为不同维度或整张卡增加简单像素风装饰（边框、图标、背景色块等）。
- **风格**：整体 UI 为像素风（字体、按钮、卡片、背景等符合像素美术风格）。
- **可选增强**：
  - 抽卡时有简单动效（如卡片翻转、光效、音效）。
  - 支持「再抽一次」或「保存/复制本页设定」等操作。
- **小程序特有能力（建议支持）**：
  - **分享**：将当前抽卡结果或小程序卡片分享给好友/群聊（`wx.showShareMenu`、自定义分享内容）。
  - **保存图片**：将当前 OC 设定卡生成图片，用户保存到相册后便于发朋友圈或收藏（`canvas` 绘制 + `wx.saveImageToPhotosAlbum`）。
  - **复制文案**：一键复制本次设定的文字版（姓名、种族等拼接成一段话），便于粘贴到笔记或聊天。

### 2.4 数据与配置

- 每个维度对应一个「选项池」：姓名池、种族池、发色池、瞳色池、性格池、怪癖池。
- 选项池应 **可配置**（如通过配置文件或后台维护），便于后续增删改选项而无需改代码。
- 随机算法：从各池中 **等概率** 随机抽取 1 项即可；若后续需要「稀有度」，可在文档中单独约定规则。

---

## 三、非功能需求

### 3.1 平台与技术

- **运行环境**：微信小程序，在微信客户端内打开使用。
- **技术栈**：小程序原生框架（WXML + WXSS + JavaScript/TypeScript），或 uni-app / Taro 等跨端框架（输出微信小程序）。
- **数据存储**：选项池可放在小程序包内（如本地 JSON）；若选项较多或需动态更新，可考虑云开发数据库或自建接口。

### 3.2 界面与风格

- **整体风格**：像素风（pixel art style）。
  - 建议：像素字体、点阵图标、低分辨率感或块状 UI 元素。
- **布局**：主区域突出「抽卡按钮」与「结果卡片」，信息层级清晰；符合竖屏手机阅读习惯。
- **适配**：
  - 以 **手机端** 为主：适配不同机型分辨率（如 375×667、390×844 等）及安全区域（刘海、底部横条）。
  - 参考 [微信小程序设计指南](https://developers.weixin.qq.com/miniprogram/design/)，保持导航、按钮等符合平台规范。

### 3.3 性能与兼容

- 抽卡与结果展示响应及时，无明显卡顿。
- 选项池规模为「数十至上百条」时，随机逻辑在前端即可快速完成，无需依赖服务端。
- **兼容**：约定最低基础库版本（如 2.10.0+），保证 iOS / Android 微信客户端正常运行；若使用 `canvas` 生成图片，需注意部分机型的兼容与权限（相册写入需用户授权）。

### 3.4 可维护性

- 随机维度与选项池与展示逻辑解耦，便于后续增加新维度或新选项。
- 若有多语言或多主题需求，可在后续版本中在文档中补充。

---

## 四、用户操作流程（简要）

```
从微信打开小程序 → 进入首页，看到像素风主界面与「抽卡」按钮
    ↓
点击「抽卡」
    ↓
系统从 6 个维度各随机 1 项，生成一张 OC 设定
    ↓
在页面中以卡片形式展示：姓名、种族、发色、瞳色、性格、怪癖
    ↓
用户可再次点击「抽卡」获得新结果，或（若实现）复制/保存当前设定
```

---

## 五、数据与规则说明（示例）

以下为各维度选项池的 **示例**，实际内容可按项目需要增删改。

- **姓名**：若干预设名字（可区分男女/中性），或简单随机组合（姓 + 名）。
- **种族**：人类、精灵、兽人、龙族、机械、魔族、仙族、吸血鬼等。
- **发色**：黑、棕、金、银白、红、蓝、绿、紫、粉、渐变、挑染等。
- **瞳色**：黑、棕、琥珀、蓝、绿、紫、红、金、异瞳等。
- **性格**：外冷内热、话痨、腹黑、天然呆、傲娇、温柔、暴躁、理性、中二等。
- **怪癖**：咬笔、数楼梯、收集纽扣、必须对称、洁癖、夜猫子、总摸头发等。

随机规则：**每个维度独立、等概率随机**；各维度之间不绑定（即不要求「某种族只能配某发色」等，除非后续版本明确需求）。

---

## 六、后续可扩展方向（可选）

- 稀有度：部分选项标记为「稀有」，抽中时特殊展示或音效。  
- 卡面图：为部分组合配置像素立绘或卡面图。  
- 历史记录：保存最近 N 次抽卡结果，支持回顾与导出。  
- 自定义选项池：用户上传或编辑自己的姓名/种族/性格等池子。  
- 多语言：界面与选项池的多语言切换。  

---

## 七、交付物预期

- **微信小程序代码包**：可在微信开发者工具中运行，实现一次抽卡 → 6 维度随机 → 像素风卡片展示；具备分享、复制文案等能力（保存图片为可选）。  
- **选项池数据**：至少每个维度具备一定数量的示例数据，保证抽卡结果多样。  
- **简要说明**：如何配置/修改各维度选项池（如改 JSON 或配置文件）。  
- **上线相关**：如需正式发布，需完成小程序注册、配置服务器域名（若用网络请求）、提交审核并遵守微信小程序运营规范。

---

## 八、微信小程序相关说明（参考）

- **权限**：若提供「保存设定卡图片到相册」，需在 `app.json` 中声明 `scope.writePhotosAlbum`，并在首次使用前通过 `wx.authorize` 或按钮弹窗引导用户授权。  
- **分享**：通过 `onShareAppMessage` 设置分享标题、图片、路径，可把当前结果 id 或文案写入 path 的 query，方便好友打开后看到同一张设定（若做「分享结果页」）。  
- **审核**：内容需符合微信小程序平台规范，选项池中避免违规、敏感词；像素风素材需确保无侵权。  

---

**文档版本**：v1.1  
**最后更新**：2025-01-29  
**产品形态**：微信小程序  

如有新需求（如新增维度、绑定规则、稀有度等），可在本文档中追加章节或修订上述条款。
